enum ProfileSelectedSection {
  infoSection,
  settingsSecion,
  contactsSection,
}

enum CommentsRatingsSelectedSection {
  commentsSection,
  ratingsSection,
}

enum ReadingsStatisticsSelectedSection {
  readingsSection,
  statisticsSection,
}

enum BookScreenSelectedSection {
  bookSection,
  commentsSection,
  recommendedSection,
}
